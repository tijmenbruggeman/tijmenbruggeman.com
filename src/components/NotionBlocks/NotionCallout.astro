---
import NotionText from '@/components/NotionBlocks/NotionText.astro';
import Debug from 'astro/debug';

const { block } = Astro.props;
---
<style>
.notion-callout {
    background: #dfdfdf;
    border-radius: 10px;
    padding: 30px;
    max-width: 550px;
}
</style>
<div class="notion-callout">
    {block.callout.text.map(t => <NotionText block={t} />)}
</div>
<Debug {Astro.props} />